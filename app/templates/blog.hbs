<!-- {{#each model.members as |member|}}
    <h1>{{member.username}}</h1>

    {{#each member.blogs as |blog|}}
        <h3>{{blog.postTitle}}</h3>
    {{/each}}

{{/each}}

<h1>BLOG.HBS STOP</h1> -->

{{#paper-switch value=booleanProp1 onChange=(action (mut booleanProp1))}}
   <!-- {{booleanProp1}} -->
   <span>Template</span>
 {{/paper-switch}}



{{#if booleanProp1}}

    {{template-2 member=model.members}}

{{else}}

    {{template-1 member=model.members}}

{{/if}}


<!-- {{template-3 data=model}} -->
<!-- <div class="layout-row layout-xs-column">
    {{#each model as |blogPost|}}
        {{dash-board blog=blogPost}}
    {{/each}}
</div>
